import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from 'react-hot-toast'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'BEAM Band - Music, Community, Impact',
  description: 'Join the BEAM Band movement across cities. Attend concerts, support fundraising goals, and make a difference in your community.',
  keywords: 'BEAM Band, music, concerts, fundraising, community, Orlando, Nashville, Atlanta, Augusta, Knoxville, Tampa, Jackson, Virginia, Los Angeles',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster 
          position="bottom-center"
          toastOptions={{
            duration: 4000,
            style: {
              background: '#363636',
              color: '#fff',
            },
          }}
        />
      </body>
    </html>
  )
}
